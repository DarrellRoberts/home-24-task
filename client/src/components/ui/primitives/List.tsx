import styled from "@emotion/styled"
import Flex from "./Flex"

export const UList = styled(Flex)`
  list-style: none;
  margin: 0;
  padding: 0;
  justify-content: space-evenly;
  flex-direction: column;
  gap: 1rem;
`
export const ListItem = styled.li`
  margin: 0;
`
